@using PracTest.Client.Components
<EditForm Model="@tasks" OnValidSubmit="@OnValidSubmit">
    <DataAnnotationsValidator />
    <ValidationSummary />
    <fieldset disabled ="@Disabled">


        <div class="form-group">
            <label>Speed Value: </label>
            <div>
                <InputNumber @bind-Value="@tasks.SpeedValue" class="form-control" />
                <ValidationMessage For="@(() => tasks.SpeedValue)" />
            </div>
        </div>

      
    </fieldset>


    @if (!Disabled)
    {
        <button type="submit" class="btn btn-success btn-block">
            @ButtonText
        </button>
    }
    <BackToList Target="tasks" />

</EditForm>

@code {
    [Parameter] public bool Disabled { get; set; } = false;
    [Parameter] public Speed? tasks{ get; set; }
    [Parameter] public string ButtonText { get; set; } = "Save";
    [Parameter] public EventCallback OnValidSubmit { get; set; }

}